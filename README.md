# Best-AVL
A self-balancing Binary Search Tree as an AVL Tree. The app will add, remove, calculate, traverse, and find data or nodes with data. It will read random, unsorted integers and calculate performance analysis. Data written to a .CSV file to be used in Excel to analyze the data samples.
